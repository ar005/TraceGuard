version: "3.9"

services:
  edr-ui3:
    build:
      context: ./edr-ui-new
      args:
        NEXT_PUBLIC_BACKEND_URL: http://localhost:8080
    restart: unless-stopped
    ports:
      - "5002:5002"